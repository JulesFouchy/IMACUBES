#include "Maths.hpp"

#include <cmath>

float MyMaths::cos(float x) {
	return std::cos(x);
}

float MyMaths::sin(float x) {
	return std::sin(x);
}

float MyMaths::pow(float x, float p) {
	return std::pow(x, p);
}